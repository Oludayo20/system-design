{
  "name": "bookhive-gateway",
  "version": "1.0.0",
  "description": "BookHive API Gateway - routes /auth, /books, /orders, /notifications to their owning services, rate-limits per IP, and logs every request. Deliberately plain Express, not NestJS.",
  "private": true,
  "license": "MIT",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "start:dev": "ts-node src/index.ts",
    "test": "echo \"no unit tests for the gateway - see README Try it walkthrough\" && exit 0"
  },
  "dependencies": {
    "express": "^4.21.2",
    "http-proxy-middleware": "^2.0.7"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.17.9",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.2"
  }
}
