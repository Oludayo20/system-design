# --- App ---
NODE_ENV=development
PORT=3011

# --- PostgreSQL ---
# Single schema, single app. Layering happens inside the codebase (Presentation ->
# Application -> Domain -> Data Access), not across services or databases.
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=library
POSTGRES_PASSWORD=library_dev_password
POSTGRES_DB=library
